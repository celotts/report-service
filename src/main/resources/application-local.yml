spring:
  datasource:
    url: jdbc:postgresql://patient-db:5432/patient  # <-- CAMBIADO
    username: patient
    password: 0513Appointments
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect